trigger:
- main   # or your default branch

pool:
  name: Edureka Session Demo.   # 👈 the agent pool you created in DevOps
  demands: agent.os -equals Linux

steps:


- script: |
    echo "Node.js version:"
    node -v
    echo "NPM version:"
    npm -v
    echo "OS info:"
    uname -a
  displayName: "Check installed tools"
